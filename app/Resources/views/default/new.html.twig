{% extends 'base.html.twig' %}

{% block body %}
    <hr/>
    <a href={{ path('test_number') }} class='waves-effect waves-light btn'>Orqaga</a>
    <div class="row">
        <div class="row">
            <h4 class="center-align">Ro`txatga kiritish</h4>
            {{ form_start(form) }}
            <div class="col s10">
                {{ form_widget(form) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}